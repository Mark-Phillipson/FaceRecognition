
Semi-functional face recognition system within the Voice Admin Application. Located at: [https://github.com/Mark-Phillipson/VoiceLauncherBlazor/blob/3bb70cd83f306084f3e71ec9aac5561b642f7c6b/RazorClassLibrary/Pages/FaceRecognition.razor.cs#L15]

 Plan an application: Blazor PWA (Progressive Web App) - Static web app with face recognition functionality.

  Currently the images are stored in a SQLite database So can we create a completely separate database file? In this project. And that would reside on the client.

    Or if that is not possible. Could we? Store the images in the local storage.

     We need to make the data entry for the names. And uploading the images. As user friendly as possible.

      We would also need a quiz-like feature where... The user is presented with. Different faces from the image. And has to enter the name they think it is. Or click a name from A. List of names.

       Also, we could have another quiz the other way round, so we would show a number of faces. And a single name. And the user would have to click the correct face.

       Can we build a plan? And troubleshoot any problems before seeing.